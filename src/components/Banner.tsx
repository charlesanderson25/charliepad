import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { faNotebook } from "@fortawesome/free-solid-svg-icons";

const Banner = () => {
  return (
    <div>
      <FontAwesomeIcon icon={faNotebook} />
    </div>
  );
};

export default Banner;
